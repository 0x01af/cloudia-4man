---
# based on:
# - https://cert-manager.io/docs/installation/kubectl/
# Usage for Ingress with let's encrypt:
# - https://cert-manager.io/docs/tutorials/acme/nginx-ingress/
#====
# - name: "deploy prerequisites"
#  kubernetes.core.k8s:
#    state: present
    # src: 'rauthy-prerequisites.yaml'
    # definition: |-
    # template: 'rauthy-prerequisites.yaml.j2'
    # - path: 'file1.j2'
    # - path: 'file2.j2'

- name: "k8s_app home-assistant"
  when: _k8s_app.version != 'none'
  block:
    - name: "deploy prerequisites of home-assistant"
      kubernetes.core.k8s:
        state: present
        template: 'home-assistant-prerequisites.yaml.j2'
    - name: "deploy home-assistant"
      kubernetes.core.k8s:
        state: present
        template: 'home-assistant.yaml.j2'
      register: _home-assistant-deployment
    - name: "force home-assistant recreation (Sledgehammer)"
      when: _home-assistant-deployment.changed
      kubernetes.core.k8s:
        state: absent
        api_version: v1
        kind: Pod
        namespace: "{{ _k8s_app.namespace }}"
        name: "{{ _k8s_app.name }}-0"
        wait: yes
        wait_timeout: 60
    - name: "configure ingress for home-assistant"
      kubernetes.core.k8s:
        state: present
        template: 'home-assistant-ingress.yaml.j2'
