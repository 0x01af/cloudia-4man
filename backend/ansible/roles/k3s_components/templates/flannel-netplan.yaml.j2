{{ ansible_managed | comment }}

network:
  version: 2
  renderer: networkd
  ethernets:
    {{ k3s.network.flannel.iface }}:
      accept-ra: false
      dhcp6: false
      dhcp4: false
      addresses:
        - "{{ _flannel_iface_ip }}/{{ _flannel_iface_subnet }}"
