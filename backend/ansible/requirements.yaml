---
# ansible collections (will be used by ansible-galaxy)
collections:
  - name: community.general
    version: 12.3.0
  - name: kubernetes.core
    version: 6.2.0

# ansible roles (will be used by ansible-galaxy)
roles:
  - name: cloudnative-pg
    src: git+https://github.com/0x01af/ansible-role-cloudnative-pg.git
    version: 0.0.1
